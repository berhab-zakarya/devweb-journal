# API Laravel : utiliser le meme host que dans le navigateur (tout 127.0.0.1 ou tout localhost).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1

# Cote backend : definir CORS_ALLOWED_ORIGINS et SANCTUM_STATEFUL_DOMAINS pour ce port (ex. :3000).